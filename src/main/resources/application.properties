spring.application.name=email-ai-assistant

# CSS tokens from user (kept as comments to avoid breaking properties format)
# p.p1 {margin: 0.0px 0.0px 0.0px 0.0px; font: 13.0px 'Helvetica Neue'; color: #000000}
# p.p2 {margin: 0.0px 0.0px 0.0px 0.0px; font: 13.0px '.AppleSystemUIFontMonospaced'; color: #000000}

# Telegram bot token — do NOT store secrets in source control.
# Recommended: set the environment variable `TELEGRAM_BOT_TOKEN` on the host or CI.
# Example (macOS / Linux): `export TELEGRAM_BOT_TOKEN=8646181185:AAFlTOsRduixchegyOP_Uhj-z_lUf91Rf6c`
# The actual token provided was omitted from the file for security.

# Application property set from user-provided token (in-file secrets are not recommended).
# If possible, prefer environment variable `TELEGRAM_BOT_TOKEN` instead of storing secrets in source.
telegram.bot.token=8646181185:AAFlTOsRduixchegyOP_Uhj-z_lUf91Rf6c
